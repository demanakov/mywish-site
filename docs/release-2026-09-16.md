# MyWish – выпуск 16 сентября 2026 года

Автор: demanakov. Статус: опубликовано.

Новая главная работает на [mwsh.ru](https://mwsh.ru/). Основа – версия Дмитрия
`d1f27565e5413a254c0cd266d19c72907846165d`. Переключение выполнено 16 сентября
в 20:14:36 по Москве. [Прежняя версия](https://mwsh.ru/previous/) сохранена отдельно
с формами, внутренними переходами и страницей благодарности; раздел закрыт от индексации.
Его цены относятся к исторической версии.

## Что сделано

- Сохранены дизайн, актуальные пакеты, цены, меню, календарь и декабрьские ставки Дмитрия.
  Плавающий Руби на новой главной отсутствует.
- Все точки входа новой главной и каталога ведут к общей форме. Она отправляет данные
  на существующий `/api/leads.php`, сохраняет согласие, UTM, yclid и ClientID.
- Успех показывается после сохранения заявки сервером. Ошибка оставляет поля заполненными;
  повторные клики и повтор запроса с тем же идентификатором не создают дубликат.
- Сохранены существующие заявки, закрытая конфигурация, PHP-обработка, amoCRM и Telegram.
  `/prazdnik/`, `/thanks/`, статьи, отдельные страницы залов и служебные разделы не удалены.
- Для 26 прежних страниц сохранены переходы на новое приложение. Sitemap, robots,
  старый рекламный редирект и проверочный файл Яндекса сохранены.

## Проверка

| Проверка | Результат |
| --- | --- |
| ESLint, TypeScript, статическая сборка | Успешно |
| Модульные тесты | 7 из 7 |
| Изолированные сценарии API | 18 из 18 на PHP 5.6.40; внешние уведомления отключены |
| Одновременный повтор запроса | 8 запросов создают одну запись |
| Новая главная: 1280×900, 477×844, 360×800 | Локальные браузерные проверки пройдены, горизонтального скролла нет |
| Календарь, залы, депозитные окна | 12 месяцев, выбор зала, все три окна; декабрьский расчёт 172 500 ₽ подтверждён |
| Прежняя версия | Локальная копия опубликованного кода: формы, благодарность, каталог, документы на трёх размерах |
| HTTPS | 20 адресов проверены: страницы доступны, API отклоняет GET, редирект сохраняет UTM |
| Сервер после загрузки | 641 файл совпадает с проверенной сборкой по SHA-256; SQLite integrity check – ok |
| Живая отправка | HTTP 201, заявка № 16; amoCRM и Telegram подтвердили доставку |

Браузерные сценарии использовали подменённый ответ API и не создавали сделок.
Один отдельный запрос прошёл настоящий production-контур: техническая запись SQLite `id=43`,
Telegram `message_id=55`, amoCRM `lead_id=6749899`. Сделка дополнительно перечитана через API.
Контакт синтетический, имя начинается с «Тест», UTM: `site_qa / test / main_20260916`.
Эту запись нужно исключать из числа клиентских и квалифицированных лидов.

Визуальная проверка выполнена на локальной итоговой сборке. Повторный полный браузерный
прогон публичного адреса с этого компьютера не завершился из-за сетевых тайм-аутов.
Публикация проверена через HTTPS/API с сервера и сравнением файлов. VPN и настройки сети не менялись.

## Резервные копии и откат

Каталог на Beget: `/home/m/mywishd7/mywish/backups/main-dmitry-20260916-1947/`.

- `production-files.tar.gz` – прежние публичные файлы и закрытые серверные файлы.
- `leads.sqlite3`, `data.sqlite` – консистентные снимки SQLite.
- `leads-before-switch.sqlite3` – дополнительный снимок непосредственно перед переключением.
- `manifest.json` – контрольные суммы и состояние перед выпуском.

Каталог закрытый. Секреты и базы не включены в передаваемый пакет.
Откат интерфейса выполняется из файловой копии; рабочую базу вместе с ним не откатывать,
чтобы не потерять новые обращения. Старые `/media/`, `/fonts/`, `/ruby-lab/` нужны сохранённому сайту.

## Сохранённые дополнительные страницы

Старые страницы отдельных залов `/halls/<зал>/` и `/food/` сохранены вместе с прежним содержимым и ценами. Актуальный прайс этого выпуска находится на новой главной, `/halls/` и `/menu/`. Для устранения расхождений на всём историческом контенте потребуется отдельное обновление старых страниц; их содержание не считается новым подтверждённым оффером.

## Передача и следующий шаг

Исходники с интеграцией и инструкция находятся в передаваемом архиве.
Подробный контракт формы: `docs/crm-and-deploy.md`. GitHub хранит код, SSH доставляет сборку
на хостинг; автоматический выпуск после push не настроен. Изменения в удалённый GitHub не отправлены.
Дмитрию нужно принять интегрированную версию в свой репозиторий и продолжать изменения из неё.
Сообщение ему подготовлено, но не отправлено. Реклама и овердрафт не запускались.

Цель выпуска – принимать обращения из обновлённого интерфейса без потери данных и источника.
Рост конверсии пока не измерен. После появления реального трафика нужно сопоставлять посещения,
сохранённые заявки, квалифицированные лиды и состоявшиеся события; технические тесты исключать.

## Отчёт руководителю

Обновил основную версию MyWish и сохранил предыдущую по отдельному адресу с резервной копией.
Подключил формы к действующей базе, amoCRM и Telegram; контрольная заявка прошла весь маршрут.
Подготовил Дмитрию исходники и инструкцию, чтобы следующие правки сохраняли интеграцию.
Влияние на конверсию проверим по реальным квалифицированным обращениям; рекламу не запускал.
