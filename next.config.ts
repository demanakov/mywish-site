import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: { formats: ["image/webp"] },
  /*
    Значок разработки Next (чёрная кнопка «N») на телефоне ложился на кнопки
    первого экрана, а свободного угла там нет: сверху звонок и меню, снизу
    «Связаться» и «Выбрать зал». Ошибки сборки и выполнения Next показывает
    и без значка. На собранном сайте значка нет в любом случае.
  */
  devIndicators: false,
  /*
    Чтобы тестировать сайт с телефона в той же Wi-Fi сети: без этого Next в
    режиме разработки блокирует служебные запросы с адреса компьютера в сети,
    и страница на телефоне не оживает. На собранный сайт не влияет.
  */
  allowedDevOrigins: ["192.168.0.131"],
};

export default nextConfig;
